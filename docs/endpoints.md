# Endpoints

This repo tries a small set of common Meteora endpoint patterns:
- /pools
- /dlmm/pools
- /v1/pools

Update `METEORA_API_BASE` and the candidate list inside:
`src/meteora/pools.ts`

If your production uses a private backend, map it there.
